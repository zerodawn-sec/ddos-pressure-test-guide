---
title: L4与L7压力测试方法对比
description: 传输层与应用层压力测试的攻击面差异、方法选择与典型误区
---

# L4与L7压力测试方法对比

## 两层的本质差异

Layer4测试打的是"运输系统"：SYN、UDP、TCP连接这些不区分业务内容的基础协议。Layer7测试打的是"业务处理"：HTTP请求、TLS握手、动态页面渲染。

一个形象的比喻：L4是堵住通往大楼的所有道路，L7是派一千个人去前台排队咨询。前者暴力直接，后者精准消耗处理能力。

## Layer4方法谱系

### 连接类

- **SYN Flood**：发送大量半开连接请求，耗尽目标TCP backlog队列。现代内核已有多层缓解（SYN Cookie），但高PPS下依然有效测试设备的转发极限。
- **TCP Connection Flood**：完成完整三次握手后保持连接，消耗文件描述符与连接跟踪表（conntrack）。
- **ATCP/PTCP变体**：绕过部分中间盒检测的TCP封装技巧。

### 流量类

- **UDP Flood**：纯带宽消耗，测试目标入口带宽与上游清洗能力。
- **UDPRAND**：随机化源端口与载荷特征，规避固定特征过滤。
- **DNS/NTP等反射放大**：借助开放解析器放大流量倍数，检验目标对伪造源IP流量的处置。
- **OVH专项**：针对法国高防机房链路特征设计的方法族。

## Layer7方法谱系

- **HTTP/HTTPS Flood**：大量合法格式的GET/POST请求，消耗应用层处理资源。
- **HTTPMIX**：混合请求路径与参数组合，避免被单一URL限速拦截。
- **TLS系列**：利用TLS握手计算成本不对称性（客户端握手便宜、服务端昂贵）。
- **CLOUD类**：穿透CDN代理层直达源站的请求构造。
- **CPU/GOD类**：针对动态渲染、数据库查询等高成本路径的定向请求。

## 选择逻辑

| 测试目标 | 推荐起点 |
|---------|---------|
| 带宽/清洗水位 | UDP类 |
| 防火墙连接表 | SYN / TCP Flood |
| Web服务承载 | HTTP / HTTPS |
| CDN+源站整体链路 | CLOUD / HTTPMIX |
| 高防机房真实强度 | OVH专项 + 混合 |

## 典型误区

1. **只用单一方法测**：防护设备对不同层的处置能力差异巨大，混合测试才暴露短板。
2. **忽略PPS只看带宽**：1Gbps小包的PPS可以是1Gbps大包的数十倍，设备瓶颈往往在PPS。
3. **L7测试不做基线对比**：没有正常流量的响应时间基线，压测数据无法解读。

完整的20种方法实测与参数细节见 [破晓压力测试平台方法库](https://zerodawnsec.com/all-methods.html)。
